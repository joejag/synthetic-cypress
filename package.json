{
  "name": "synthetic-cypress-example",
  "version": "1.0.0",
  "description": "Run Cypress via its module API inside an express server",
  "scripts": {
    "start": "node ./e2e-tests"
  },
  "dependencies": {
    "cypress": "^5.6.0",
    "express": "^4.17.1",
    "node-cron": "^2.0.3",
    "serve-index": "^1.9.1"
  },
  "devDependencies": {
    "eslint": "^7.13.0"
  }
}
